--- support/Makefile.orig	2014-09-18 04:08:33.000000000 +0000
+++ support/Makefile	2014-10-26 08:22:18.000000000 +0000
@@ -57,7 +57,7 @@
 	cd $(INSTALLDIR); $(RANLIB) lib$(LIBNAME).$(A)
 	cd $(INSTALLDIR); chmod 644 $(PUB) lib$(LIBNAME).$(A)
 	if test -f dll$(LIBNAME)$(EXT_DLL); then \
-	   cp dll$(LIBNAME)$(EXT_DLL) $(STUBLIBDIR)/; fi
+	   cp dll$(LIBNAME)$(EXT_DLL) $(STAGEDIR)$(PREFIX)/lib/ocaml/stublibs; fi
 
 installopt:
 	@if test -d $(INSTALLDIR); then : ; else mkdir $(INSTALLDIR); fi
